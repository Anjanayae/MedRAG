# --- Step 1: ingestion & chunking ---
pandas
lxml
tqdm
pytest

# --- Step 2: embeddings & vector store ---
sentence-transformers==5.6.0
chromadb==1.5.9

# --- Step 3+4: hybrid retrieval, reranking, generation, API ---
rank-bm25==0.2.2
fastapi==0.139.0
uvicorn==0.51.0
groq==1.5.0
python-dotenv==1.2.2
httpx==0.28.1

# --- Step 8: frontend ---
streamlit==1.60.0
requests